```git
user.name=
user.email=
core.autocrlf=input # windows 为true
alias.lg=log --all --oneline --graph # git config --global alias.lg "log --all --oneline --graph"
http.proxy=http://127.0.0.1:80 # VPN端口
```
